# Templates

Standalone HTML delivery is produced by `scripts/package_html.py` (also `sculpt.py package`).

It mirrors `web-viz-libraries` Three.js **0.160** importmap + OrbitControls and sets
`preserveDrawingBuffer: true` for `render_html` screenshots.

Do not hand-copy a second HTML scaffold unless the packager cannot run.
