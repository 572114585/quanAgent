ddgs
deepagents
langchain-openai
langfuse
wecom-aibot-python-sdk
httpx
requests
boto3
markdownify
qrcode
pycryptodome
python-dotenv
openpyxl
python-docx
pandas
Pillow
PyPDF2
fpdf2
playwright
markdown
pymdown-extensions
pypdf
fastapi>=0.110
uvicorn[standard]>=0.27
sse-starlette>=2.0
python-multipart>=0.0.9
pydantic>=2.5
langgraph-checkpoint-sqlite>=2.0
pytest>=8.0
pytest-asyncio>=0.23

# PPT Master is vendored at a locked upstream revision; its optional workflow
# dependencies are installed through this explicit include.
-r workspace/skills/ppt-master/requirements.txt

# MinerU 文档解析走云 API(复用 skills/mineru/scripts/extract.py),无需本地 torch/GPU
# 前置依赖:Node.js + `npm install -g mineru-open-api`
# token 配置:在 .env 设置 MINERU_API_TOKEN(可选,无 token 自动降级 flash-extract)
# mineru>=1.0
